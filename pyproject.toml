[project]
name = "issuematch"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.110", "httpx>=0.27", "PyJWT[crypto]>=2.8", "pydantic>=2.0", "pytest>=8.0", "pytest-asyncio>=0.23",  "boto3>=1.40"]

[tool.pytest.ini_options]
testpaths = ["tests"]
